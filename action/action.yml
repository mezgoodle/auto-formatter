name: "Hello Actions"
description: "Greet someone"
author: "mezgoodle"

inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.myInput }}

branding:
  icon: "mic"
  color: "purple"
