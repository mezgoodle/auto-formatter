name: "Hello Actions"
description: "Greet someone"
author: "mezgoodle"

inputs:
  GITHUB_TOKEN:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.GITHUB_TOKEN }}

branding:
  icon: "mic"
  color: "purple"
