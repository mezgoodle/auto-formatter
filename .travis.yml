language: python
python:
  - 3.7

services:
  - docker

install:
  # - docker run -d -p 127.0.0.1:80:80 --name blog blog

# before_script:
#   - pip install -r requirements.txt
#   - pip install mock
#   - pip install requests
#   - pip install feedparser

script:
  - cd action
  - docker build -t action Dockerfile.test
  - docker ps
  - docker container run --name web action
  # - python tests.py
